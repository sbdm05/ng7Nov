import { TotalClientPipe } from './total-client.pipe';

describe('TotalClientPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalClientPipe();
    expect(pipe).toBeTruthy();
  });
});
